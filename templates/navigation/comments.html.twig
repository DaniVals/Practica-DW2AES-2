<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
		{% include "content.html.twig" %}
		<link rel="stylesheet" href="{{asset('css/thread.css')}}">
		<link rel="stylesheet" href="{{asset('css/userProfile.css')}}">
		
		<script src="{{asset('js/loadComments.js')}}"></script>

        <title>Comments</title>
		{# TODO: poner nombre #}
		{# targetPost #}
    </head>
	{% include "header.html.twig" %}
    <body>
		<input type="hidden" id="ajaxRoute" value="{{ path('load_comments_ajax') }}">
		<input type="hidden" id="createPostRoute" value="{{ path('new_comment', {'post': idPost, 'comment': 0}) }}">
		<div class="post" id="{{ idPost }}">
		</div>
    </body>
</html>
