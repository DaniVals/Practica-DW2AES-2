<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
		{% include "content.html.twig" %}
		<link rel="stylesheet" href="{{asset('css/thread.css')}}">
		<link rel="stylesheet" href="{{asset('css/postPhoto.css')}}">
        <title>Nuevo Post</title>
    </head>
	{% include "header.html.twig" %}
    <body>
		<div class="post">
		
			{% if error != "" %}
				<p>{{error}}</p>
			{% endif %}

			<h2>¿Qué hay de nuevo?</h2>
			<form action="{{ path('new_post') }}" method="post" enctype="multipart/form-data">

				<input type="file" name="postPhoto" id="postPhoto" required>
				<br>

				<input type="submit" value="Subir Foto" id="submitPost">
			</form>

		</div>
    </body>
</html>
