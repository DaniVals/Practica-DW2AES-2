<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
		{% include "content.html.twig" %}
		<link rel="stylesheet" href="{{asset('css/thread.css')}}">
		<link rel="stylesheet" href="{{asset('css/userProfile.css')}}">
        <title>@{{targetProfile.userName}}</title>
		{# targetProfile #}
    </head>
	{% include "header.html.twig" %}
    <body>
		<div id="profileHeader">
			<img src="{{asset('userData/'~targetProfile.idUser~'/ProfilePicture.png')}}" alt="ProfilePicture">
			<!-- idUser -->
			<h1>{{targetProfile.userName}}</h1>
			<p>{{targetProfile.bio}}</p>
			<p>Seguidores: <span>{{targetProfile.followers}}</span> Siguiendo: <span>{{targetProfile.following}}</span></p>
		</div>
    </body>
</html>
