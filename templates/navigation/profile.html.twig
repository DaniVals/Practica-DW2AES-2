{# templates/login.html.twig #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
		{% include "content.html.twig" %}
		<link rel="stylesheet" href="{{asset('css/thread.css')}}">
		<link rel="stylesheet" href="{{asset('css/userProfile.css')}}">
        <title>Perfil</title>
		{# esto deberia poner el nombre del usuario que estas buscando #}
		{# targerUser #}
    </head>
	{% include "header.html.twig" %}
    <body>
		<div id="profileHeader">
			<img src="{{asset('userData/{{targetUser.idUser}}/ProfilePicture.png')}}  ../userData/0/ProfilePicture.png" alt="ProfilePicture">
			<!-- idUser -->
			<h1>{{targetUser.idUser}}</h1>
			<p>bio</p>
			<p>Seguidores: <span>F</span> Siguiendo: <span>F</span></p>
		</div>
    </body>
</html>
